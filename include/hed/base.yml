schema: lib
name: base
version: 0.0.1
doc: base structure
prefix: hed_

headers:
  - <hed/priv/config.h>

structures:
  -
    name: name
    type: str
    size: 40
    pattern: "[a-zA-Z][a-zA-Z0-9_]*"
